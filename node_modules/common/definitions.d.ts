import { Col } from "common/col"

export interface IApiResult<T>{
   statusCode: number
   message: string
   result: T
}

export interface ITableApi{
   cols(schema: string, table: string) : IApiResult<Col[]>
   beans(schema:string, table: string, fields: string[]) : IApiResult<any[]>
   bean(schema:string, table: string, id: string, fields: string[]) : IApiResult<any>
}